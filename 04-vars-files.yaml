- name: variables from files 
  host: local
  connection: local
  vars_files:
  - course.yaml
  tasks:
  - name: print course information
    ansible.builtin.debug:
      msg: "print the course info {{ COURSE }}, Duratio {{ DURATION }}, trainer{{ TRAINER }}, fee{{ FEE }}"
